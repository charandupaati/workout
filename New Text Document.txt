const passport = require("passport");
const GoogleStrategy = require("passport-google-oauth20").Strategy;
require("dotenv").config(); // Load environment variables

passport.use(
  new GoogleStrategy(
    {
      clientID: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.const passport = require("passport");
const GoogleStrategy = require("passport-google-oauth20").Strategy;
require("dotenv").config(); // Load environment variables

passport.use(
  new GoogleStrategy(
    {
      clientID: process.env.274854955813-uj5j1ip0lhqlq3p5n3v6uto8opea576p.apps.googleusercontent.com,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
      callbackURL: process.env.http://localhost:8088/auth/google/callback,
      scope: ["profile", "email", "https://www.googleapis.com/auth/fitness.activity.read"],
    },
    (accessToken, refreshToken, profile, done) => {
      profile.accessToken = accessToken; // Store access token in the user object
      return done(null, profile);
    }
  )
);

// Serialize user session
passport.serializeUser((user, done) => {
  done(null, user);
});

// Deserialize user session
passport.deserializeUser((obj, done) => {
  done(null, obj);
});
,
      callbackURL: process.env.http://localhost:8088/auth/google/callback,
      scope: ["profile", "email", "https://www.googleapis.com/auth/fitness.activity.read"],
    },
    (accessToken, refreshToken, profile, done) => {
      profile.accessToken = accessToken; // Store access token in the user object
      return done(null, profile);
    }
  )
);

// Serialize user session
passport.serializeUser((user, done) => {
  done(null, user);
});

// Deserialize user session
passport.deserializeUser((obj, done) => {
  done(null, obj);
});
